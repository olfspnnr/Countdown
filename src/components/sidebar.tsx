import * as React from "react";

export interface sideBarObject {}

interface sideBarProps {}

interface sideBarState {}

export const SideBar = (props: sideBarProps, state: sideBarState) => {
  return (
    <div id="sideBar" className="w-1/4">
      test
    </div>
  );
};
