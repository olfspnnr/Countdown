import * as React from "react";
import { topic } from "./countdown";
import { topicItem } from "./CountdownObject";

interface modalProps {}

interface modalOptions {}

export const Modal = (props: modalProps) => {
  return <div id="modal" className="" />;
};
